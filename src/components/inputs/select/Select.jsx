import React from 'react';
import PropTypes from 'prop-types';

export const Select = ({ 
  ...props 
}) => {  

    return (
      <></>
    );
};


Select.propTypes = {

};

Select.defaultProps = {
};
